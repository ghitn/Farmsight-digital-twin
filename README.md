# Crop Digital Twin MVP

## Setup
1. Install Python (>=3.7) and pip.
2. Install dependencies:
   ```
   pip install -r requirements.txt
   ```
3. Set your OpenWeatherMap API key in `config.py`.
4. Run the app:
   ```
   python app.py
   ```
5. Visit http://127.0.0.1:5000/ in your browser.
